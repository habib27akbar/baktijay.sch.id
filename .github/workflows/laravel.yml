on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.baktijaya.sch.id
          username: ${{ secrets.baktijay }}
          password: "H0K9fuuA82&l"
          server-dir: /public_html/thantawi.com/project-app
